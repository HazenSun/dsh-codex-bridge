# DSH credentials remain owned by DSH. Configure providers with `dsh web` or
# ~/.dsh/settings.yaml and ~/.dsh/.credentials.yaml; do not copy secrets here.
DSH_BRIDGE_CONFIG=./bridge.yaml
DSH_BRIDGE_LOG_LEVEL=info
